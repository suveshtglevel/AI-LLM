/**
 * Barrel file — importing this triggers auto-registration of all
 * workflow definitions via the module-level WorkflowRegistry.register() calls.
 */
import './definitions/youtube-video.workflow';
import './definitions/podcast.workflow';
import './definitions/blog-post.workflow';
